apiVersion: v1
kind: Service
metadata:
  name: php-app
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: php-app
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: php-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
